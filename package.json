{
  "name": "webkurier-hybrid",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "lint": "eslint . || echo 'No eslint config, skipping lint'",
    "test": "jest --ci --passWithNoTests || echo 'No tests configured'"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}